spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mogul
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: password
  jpa:
    open-in-view: false
    generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  logging:
    level:
      org:
        springframework:
          security:
            DEBUG
jwt:
  secret: secretKey
